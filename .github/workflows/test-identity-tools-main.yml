# Purpose: Run Identity Tools' test suite (main tree) when code under 95_Identity_Tools/main changes.
# Communication / relationships: Runs on GitHub Actions when files under 95_Identity_Tools/main change,
#   when this workflow file itself changes, or on manual dispatch from the Actions tab.
#   Reports pass/fail back to the commit/PR; does not call any other workflow.
# Settings / parameters: Triggers on push/pull_request to `main` touching `95_Identity_Tools/main/**` or
#   `.github/workflows/test-identity-tools-main.yml`, plus manual `workflow_dispatch`. Python pinned to 3.14.
#   Working directory scoped to `95_Identity_Tools/main`. PYTHONPATH set to that directory so top-level
#   packages inside it import absolutely, matching local runs.
# Edge cases: pytest exit code 5 ("no tests collected") is treated as a pass so projects
#   without a test suite report green. Real failures (exit 1) and collection/import errors
#   (exit 2) still fail the job.
# Known limitations: Installs only pytest, not project dependencies. Does not cache pip,
#   lint, type-check, or build artifacts. A project that silently loses all its tests will
#   still report green because of the exit-5 rule.
# Examples: Editing anything under `95_Identity_Tools/main/` and pushing triggers this run; editing this
#   workflow file also triggers it, so changes can be validated immediately.

name: Test Identity Tools (main)

on:
  push:
    branches: [main]
    paths:
      - '95_Identity_Tools/main/**'
      - '.github/workflows/test-identity-tools-main.yml'
  pull_request:
    branches: [main]
    paths:
      - '95_Identity_Tools/main/**'
      - '.github/workflows/test-identity-tools-main.yml'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: 95_Identity_Tools/main
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.14'

      - name: Install pytest
        run: |
          python -m pip install --upgrade pip
          pip install pytest

      - name: Run tests
        run: |
          export PYTHONPATH="$PWD:$PYTHONPATH"
          set +e
          pytest -q
          code=$?
          set -e
          if [ "$code" -eq 5 ]; then
            echo "::notice::No tests collected in 95_Identity_Tools/main - treating as pass."
            exit 0
          fi
          exit "$code"
